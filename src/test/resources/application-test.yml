spring:
  application:
    name: contact-management-system-test
  datasource:
    url: jdbc:h2:mem:cms_test;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

jwt:
  secret: TestJwtSecretKeyForUnitAndIntegrationTests123456!!
  expiration: 86400000

server:
  port: 0
